{
  "name": "ghost-web",
  "version": "1.0.0",
  "description": "Ghost checkout and license delivery web server",
  "main": "server.js",
  "scripts": {
    "start":   "node server.js",
    "dev":     "node --watch server.js",
    "prod":    "NODE_ENV=production node server.js"
  },
  "dependencies": {
    "dotenv":     "^16.0.0",
    "express":    "^4.18.0",
    "node-fetch": "^3.3.0",
    "stripe":     "^14.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
